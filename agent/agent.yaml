spec_version: "0.1.0"
name: dbs-help
version: 1.0.0
description: Help DBS Bank India website and digibank users who get stuck on a page.

model:
  preferred: "lyzr:${LYZR_AGENT_ID}@https://agent-prod.studio.lyzr.ai/v4"
  constraints:
    temperature: 0.3
    max_tokens: 800

tools: []

runtime:
  max_turns: 6
